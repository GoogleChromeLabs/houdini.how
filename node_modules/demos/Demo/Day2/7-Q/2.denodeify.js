var fs=require('fs');
var Q=require('./q');
var readFile = Q.denodeify(fs.readFile);
readFile("message.txt", "utf-8").done(function (text) {
		console.log(text)
	},function (error) {
		console.log(error)
	});
