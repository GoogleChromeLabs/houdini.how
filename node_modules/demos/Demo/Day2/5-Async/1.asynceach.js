var async = require('./async');

var square = function (num, doneCallback) {
  console.log(num * num);
  return doneCallback();
};

async.each([1, 2, 3, 4, 5], square, function (err) {
  console.log("Finished!");
});
