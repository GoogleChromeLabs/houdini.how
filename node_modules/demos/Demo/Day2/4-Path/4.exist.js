var path=require('path');
var fs=require('fs');

var mypage = path.join(process.cwd(),"join1.js")

fs.exists(mypage, function(exists){
	console.log("Does the file exist?", exists)
})
fs.existsSync(mypage)
