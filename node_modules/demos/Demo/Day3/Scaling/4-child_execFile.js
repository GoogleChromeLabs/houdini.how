var execFile=require('child_process').execFile;

var child=execFile('node',['3-child_exec.js'],null,function(err,stdout,stderr){
if(err)
{
	console.log('Error:'+stderr);
}
else
{
	console.log('Output is '+stdout);
}
});

console.log("PID is "+child.pid);

